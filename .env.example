# Copy to .env and edit. Values here are picked up by loopback_speed.py and
# pytest when --port is not given. .env is gitignored.
#
# Find your port with:  python loopback_speed.py --list-ports
#
# Leave unset to auto-detect an attached Pico by USB VID:PID.

PICOUART_PORT=COM11
